package com.unfollowlens.ui.theme

import androidx.compose.ui.graphics.Color

// ── Background ──────────────────────────────────────────────────────
val BgBase = Color(0xFF0B0C10)
val BgSurface = Color(0xFF14161C)
val BgSurfaceElevated = Color(0xFF1B1E26)

// ── Glass & Stroke ──────────────────────────────────────────────────
val GlassOverlay = Color(0x0AFFFFFF)       // rgba(255,255,255,0.04)
val StrokeHairline = Color(0x14FFFFFF)     // rgba(255,255,255,0.08)

// ── Accents ─────────────────────────────────────────────────────────
val AccentPrimary = Color(0xFF7C5CFF)      // violet — primary actions
val AccentMutual = Color(0xFF3DDC97)       // mint — mutuals
val AccentFan = Color(0xFF4FA8FF)          // blue — fans
val AccentNotBack = Color(0xFFFF6B6B)      // coral — not following back

// ── Text ────────────────────────────────────────────────────────────
val TextPrimary = Color(0xFFF5F6FA)
val TextSecondary = Color(0xFF9BA0AE)

// ── Neumorphic Shadows ──────────────────────────────────────────────
val NeuLightShadow = Color(0x0DFFFFFF)     // ~5% white, top-left highlight
val NeuDarkShadow = Color(0x33000000)      // ~20% black, bottom-right shadow

// ── Utility ─────────────────────────────────────────────────────────
val SuccessGreen = Color(0xFF3DDC97)
val ErrorRed = Color(0xFFFF6B6B)
val WarningAmber = Color(0xFFFFB347)
